#include<stduo.h>
int main()
{
	struct province
	{int s;
	int d;
	int c;
	int w;
	int k;
	}pro[34];










void input(struct province pro[])
{int i;
for(i=0,i<34,i++)
{scanf("%d %d %d %d %d",&pro[i].s,&pro[i].d,&pro[i].c,&pro[i].w,&pro[i].k);
}
}

void output(struct province pro[])
{int i;
